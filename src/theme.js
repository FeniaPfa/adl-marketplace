import { createTheme } from '@mui/material/styles';

export const theme = createTheme( {
  palette: {
    type: 'light',
    primary: {
      main: '#c62828',
    },
    secondary: {
      main: '#fff',
    },
    background: {
      default: '#f6f9fc',
    },
  },
});